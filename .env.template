# 🗄️ DriveSchool Pro v3.2.0 - Environment Configuration Template
# 
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.template .env
# 2. Fill in your actual values (remove the placeholder comments)
# 3. Never commit .env to Git (it's in .gitignore)

# ============================================================================
# 🔍 POSTGRESQL DATABASE CONFIGURATION (REQUIRED)
# ============================================================================
# These variables MUST match your PostgreSQL setup

POSTGRES_HOST=localhost
POSTGRES_PORT=5432  
POSTGRES_USER=driveschool_user
POSTGRES_PASSWORD=DriveSchool2024!
POSTGRES_DATABASE=driveschool_pro_v3

# 📝 NOTES:
# - POSTGRES_HOST: Usually 'localhost' for local development
# - POSTGRES_PORT: Default PostgreSQL port is 5432
# - POSTGRES_USER: The database user you created (NOT 'postgres')
# - POSTGRES_PASSWORD: The password you set for driveschool_user
# - POSTGRES_DATABASE: The database name you created

# ============================================================================
# 🌐 APPLICATION CONFIGURATION (REQUIRED)
# ============================================================================

BASE_URL=http://localhost:3000
AUTH_SECRET=REPLACE_WITH_STRONG_SECRET_KEY

# 📝 HOW TO GENERATE AUTH_SECRET:
# Run one of these commands to generate a secure secret:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#   openssl rand -base64 32
# Then replace REPLACE_WITH_STRONG_SECRET_KEY with the generated value

# ============================================================================
# 📧 EMAIL CONFIGURATION (OPTIONAL)
# ============================================================================
# Only needed if you want to send emails from the application

RESEND_API_KEY=
# Get your API key from: https://resend.com/api-keys
# Leave empty if you don't need email functionality

# ============================================================================
# 📱 WHATSAPP CONFIGURATION (OPTIONAL) 
# ============================================================================
# Only needed if you want WhatsApp integration

WHATSAPP_BUSINESS_NUMBER=
# Format: Country code + number (e.g., 447123456789 for UK)
# Leave empty if you don't need WhatsApp integration

# ============================================================================
# 🔍 EXAMPLE COMPLETE .env FILE:
# ============================================================================
# Your actual .env should look like this (with real values):
#
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=driveschool_user  
# POSTGRES_PASSWORD=YourActualPassword123!
# POSTGRES_DATABASE=driveschool_pro_v3
# BASE_URL=http://localhost:3000
# AUTH_SECRET=xl8fJ9k2mN5pQ8rT1uY6vZ3wA7bC4dE0fG9hI2jK5lM8nO1pR4sT7uX0yZ3wA6b
# RESEND_API_KEY=re_your_actual_api_key_here
# WHATSAPP_BUSINESS_NUMBER=447123456789
#
# ============================================================================

# 🚨 SECURITY REMINDERS:
# - Never share your .env file
# - Never commit .env to Git  
# - Use strong passwords
# - Generate secure AUTH_SECRET
# - Keep API keys confidential